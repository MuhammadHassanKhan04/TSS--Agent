{
  "name": "tss-agent",
  "version": "1.0.0",
  "description": "TSS WhatsApp AI Admission Officer Agent",
  "main": "server/server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client && npm install && npm run build",
    "setup": "npm install && cd client && npm install && npm run build",
    "dev": "node server/server.js"
  },
  "keywords": [
    "whatsapp",
    "bot",
    "admission",
    "education"
  ],
  "author": "The Student Space Institute",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "multer": "^2.2.0",
    "puppeteer": "^22.0.0",
    "qrcode": "^1.5.4",
    "whatsapp-web.js": "^1.34.7",
    "ws": "^8.21.0"
  }
}
